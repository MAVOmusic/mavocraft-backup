# MAVOcraft — UPDATE GUIDE (2026-09-04: MobFarm 2.6.2 community chest fix)

Plain steps. Only replace/delete files named here. Never delete plugin folders.

## 1. STOP the server (full stop, no /reload)

## 2. DELETE these files from plugins/ (jars only)

    MAVOMobFarm-2.6.1.jar            <- replace it with 2.6.2
    MAVOMobFarm-2.6.0.jar (if still there)
    MAVOMobFarm-2.5.0.jar (if still there)
    MAVOCasino-1.2.3.jar (if still there)

Keep: every other jar. Check plugins/ has exactly ONE jar per plugin.

## 3. UPLOAD this jar into plugins/

    MAVOMobFarm-2.6.2.jar            (bay community chests are ONE double chest, face the walkway)

MAVOCasino-1.2.4.jar and MAVOPortalRoom-1.2.0.jar (fixed) stay as they are - no re-upload needed.

## 4. DELETE config: NOTHING this time

- plugins/MAVOMobFarm/config.yml + data.yml: KEEP. Boot writes the real spawner prices into config automatically.
- Plugins/MAVOGuide/config.yml: KEEP (Guide jar unchanged, still v18).
- All other MAVO configs/data: KEEP.

## 5. START the server

## 6. After start (console/OP)

    /updates reload
    /mobfarm resholo

Boot log must show:
- MAVOMobFarm (2.6.2) + "MobFarm shop prices embedded: 36 mobs (zombie pick 75000)."
- No MAVOMobFarm load error; MAVOPortalRoom (1.2.0) loads with no "Fatal error".
- MAVOCasino log says "MAVOCasino 1.2.4 enabled".

## 7. Check in game

- Hub community chest: opens normally, donations count (unchanged).
- EVERY bay community chest: ONE double chest, its lid/opening faces the walkway (NORTH), and donations from the bay chest count (message + coins to the community goal).

## 8. NEVER delete (standing rule)

plugins/EconomyShopGUI/ (sections/ + shops/ too - museum files are regenerated by /museum shopsgen),
plugins/LuckPerms/, plugins/TAB/, any MAVO plugin's data.yml / homes.yml / config.yml not named above.
