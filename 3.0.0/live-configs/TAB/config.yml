# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Header-&-Footer
header-footer:
  enabled: true
  designs:
    world-test:
      display-condition: "%world%=test"
      header:
        - "Test header in world 'test'"
      footer:
        - "Test footer in world 'test'"
    default:
      header:
        - "<gradient:#FF3333:#4477FF>&m                        </gradient>"
        - "<b>&4MAVOcraft</b> &8| &2Kick Community"
        - "<gradient:#4477FF:#FF3333>&m                        </gradient>"
      footer:
        - "&6mavocraft.my.pebble.host &8| &4mavomusic.co.uk"
        - "&7/sb &8= switch scoreboard (stream / grind)"
        - "<gradient:#4477FF:#FF3333>&m                        </gradient>"
tablist-name-formatting:
  enabled: true
  disable-condition: '%world%=disabledworld'

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Nametags
scoreboard-teams:
  enabled: true
  enable-collision: true
  invisible-nametags: false
  # https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Sorting-players-in-tablist
  sorting-types:
    - "GROUPS:owner,admin,mod,helper,builder,vip,flex,yeman,satan,god,default"
    - "PLACEHOLDER_A_TO_Z:%player%"
  case-sensitive-sorting: true
  can-see-friendly-invisibles: false
  disable-condition: '%world%=disabledworld'

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Playerlist-Objective
playerlist-objective:
  enabled: true
  value: "%ping%"
  fancy-value: "&7Ping: %ping%"
  title: "TAB" # Only visible on Bedrock Edition
  render-type: INTEGER
  disable-condition: '%world%=disabledworld'

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Belowname
belowname-objective:
  enabled: false
  value: "%health%"
  fancy-value: "&c%health%"
  fancy-value-default: "NPC"
  title: "&cHealth"
  disable-condition: '%world%=disabledworld'
  view-distance: 10

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Spectator-fix
prevent-spectator-effect:
  enabled: false

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Bossbar
bossbar:
  enabled: false
  toggle-command: /bossbar
  remember-toggle-choice: true
  hidden-by-default: false
  bars:
    ServerInfo:
      style: "PROGRESS"
      color: "RED"
      progress: "100"
      text: "Test"

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Scoreboard
scoreboard:
  enabled: true
  toggle-command: /sb
  remember-toggle-choice: true
  hidden-by-default: false
  delay-on-join-milliseconds: 0
  # Minecraft clients only show 15 sidebar lines. Keep boards ≤15.
  # Sidebar is ALWAYS on the right in Java — no left sidebar exists.
  # Stream: facecam+SubGoal top-right → short board, no huge profession list.
  scoreboards:
    # Default: stream-safe compact (under Sub Goal)
    stream:
      display-condition: "%world%!=disabledworld"
      title: "<gradient:#FF3333:#4477FF><b></b></gradient>"
      lines:
        # 2 spacers so title clears typical Sub Goal text
        - "%mavohud_tab_name%"
        - "%mavohud_level_line%"
        - "&8&m---------------"
        - "&e⛁ &f%vault_eco_balance_formatted%     &c☠ %mavohud_deaths%"
        - "&fDay &e%mavohud_day%    %mavohud_time%"       
        - "&8&m---------------"
        - "&7⛏ &f%mavoprof_miner%  &2🪓 &f%mavoprof_lumberjack% "
        - "&6⛏ &f%mavoprof_excavator%  &4⚔ &f%mavoprof_hunter% "
        - "&c🏹 &f%mavoprof_archer%  &9🛡 &f%mavoprof_guardian% "
        - "&b🎣 &f%mavoprof_fisherman%    &6♠ &f%mavoprof_gambler% "
        - "&e🌾 &f%mavoprof_farmer%    &d😴 &f%mavoprof_sleeper% "
        - "&8&m---------------"
        - "%mavohud_coords%"
        # = 14 content rows max (title separate) — under 15 limit

    # Full grind board — cycle with /sb
    grind:
      title: "&6&lProfessions"
      lines:
        - "%mavohud_tab_name%"
        - "&eLv &f%mavohud_level% &7(%mavohud_level_pct%%)"
        - "&8&m---------------"
        - "&2🪓 &f%mavoprof_lumberjack%"
        - "&7⛏ &f%mavoprof_miner%"
        - "&6⛏ &f%mavoprof_excavator%"
        - "&e🌾 &f%mavoprof_farmer%"
        - "&4⚔ &f%mavoprof_hunter%"
        - "&c🏹 &f%mavoprof_archer%"
        - "&b🎣 &f%mavoprof_fisherman%"
        - "&9🛡 &f%mavoprof_guardian%"
        - "&6♠ &f%mavoprof_gambler%"
        - "&8&m---------------"
        - "&fDay &e%mavohud_day%  &c☠ %mavohud_deaths%"
        - "%mavohud_coords%"


# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Layout
layout:
  enabled: false
  direction: COLUMNS
  default-skin: mineskin:37e93c8e12cd426cb28fce31969e0674
  enable-remaining-players-text: true
  remaining-players-text: '... and %s more'
  empty-slot-ping-value: 1000
  layouts:
    default:
      slot-count: 80
      fixed-slots:
        - '1|&3Website&f:'
        - '2|&bmyserver.net'
        - '3|&8&m                       '
        - '4|&3Name&f:'
        - '5|&b%player%'
        - '7|&3Rank&f:'
        - '8|Rank: %group%'
        - '10|&3World&f:'
        - '11|&b%world%'
        - '13|&3Time&f:'
        - '14|&b%time%'
        - '21|&3Teamspeak&f:'
        - '22|&bts.myserver.net'
        - '23|&8&m                       '
        - '41|&3Store&f:'
        - '42|&bshop.myserver.net'
        - '43|&8&m                       '
      groups:
        staff:
          display-condition: permission:tab.staff
          slots:
            - 24-40
        players:
          slots:
            - 44-80

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Ping-Spoof
ping-spoof:
  enabled: false
  value: 0

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Global-playerlist
global-playerlist:
  enabled: false
  display-others-as-spectators: false
  display-vanished-players-as-spectators: true
  isolate-unlisted-servers: false
  update-latency: false
  spy-servers:
    - spyserver1
    - spyserver2
  server-groups:
    lobbies:
      - lobby1
      - lobby2
    group2:
      - server1
      - server2

placeholders:
  date-format: "dd.MM.yyyy"
  time-format: "[HH:mm:ss / h:mm a]"
  time-offset: 0
  register-tab-expansion: false
  locale: en-US

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Placeholder-output-replacements
placeholder-output-replacements:
  "%essentials_vanished%":
    "yes": "&7| Vanished"
    "no": ""

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Conditional-placeholders
conditions:
  nick: # use it with %condition:nick%
    conditions:
      - "%player%=%essentials_nickname%"
    true: "%player%"
    false: "~%essentials_nickname%"

placeholder-refresh-intervals:
  default-refresh-interval: 500
  "%mavohud_coords%": 50
  "%mavohud_time%": 500
  "%mavohud_day%": 1000
  "%mavohud_deaths%": 1000
  "%mavohud_level%": 1000
  "%mavohud_level_pct%": 1000
  "%mavohud_level_bar%": 1000
  "%mavohud_level_line%": 1000
  "%mavohud_tab_name%": 1000
  "%mavohud_rank%": 2000
  "%mavoprof_lumberjack%": 1000
  "%mavoprof_miner%": 1000
  "%mavoprof_excavator%": 1000
  "%mavoprof_farmer%": 1000
  "%mavoprof_hunter%": 1000
  "%mavoprof_archer%": 1000
  "%mavoprof_fisherman%": 1000
  "%mavoprof_guardian%": 1000
  "%mavoprof_gambler%": 1000
  "%server_uptime%": 1000
  "%server_tps_1_colored%": 1000
  "%server_unique_joins%": 5000
  "%player_health%": 200
  "%player_ping%": 1000
  "%vault_prefix%": 1000
  "%vault_eco_balance_formatted%": 1000
  "%rel_factionsuuid_relation_color%": 1000

# assigning groups by permission nodes instead of taking them from permission plugin
assign-groups-by-permissions: false

# if the option above is true, all groups are taken based on permissions and the one higher in this list is used as primary
# Warning! This is not sorting list and has nothing to do with sorting players in tablist!
primary-group-finding-list:
  - Owner
  - Admin
  - Mod
  - Helper
  - default

# Refresh interval (in milliseconds) of:
# - Permission checks in conditions / sorting
# - Group retrieving from permission plugin for sorting / per-group properties
permission-refresh-interval: 1000

# Unlocks extra console messages
debug: false

# https://github.com/NEZNAMY/TAB/wiki/MySQL
mysql:
  enabled: false
  host: 127.0.0.1
  port: 3306
  database: tab
  username: user
  password: password
  useSSL: true

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Multi-server-support
proxy-support:
  enabled: true
  channel-name-suffix: "custom"
  # Supported types: PLUGIN, REDIS, RABBITMQ
  type: PLUGIN
  plugin:
    # Compatible plugins: RedisBungee
    # If enabled and compatible plugin is found, hook is enabled to work with proxied players
    name: RedisBungee
  redis:
    url: 'redis://:password@localhost:6379/0'
  rabbitmq:
    exchange: 'plugin'
    url: 'amqp://guest:guest@localhost:5672/%2F'

components:
  minimessage-support: true
  disable-shadow-for-heads: true

# Internal value, do not change this
config-version: 7

########################################################################
# BUKKIT ONLY - THE FOLLOWING SECTION IS ONLY FOR BACKEND INSTALLATION #
########################################################################

# https://github.com/NEZNAMY/TAB/wiki/Feature-guide:-Per-world-playerlist
per-world-playerlist:
  enabled: false
  # players with tab.staff will always see all players
  allow-bypass-permission: false
  # players in these worlds will always see all players
  ignore-effect-in-worlds:
    - ignoredworld
    - build
  shared-playerlist-world-groups:
    lobby:
      - lobby1
      - lobby2
    minigames:
      - paintball
      - bedwars

compensate-for-packetevents-bug: false

#####################################################################
# PROXY ONLY - THE FOLLOWING SECTION IS ONLY FOR PROXY INSTALLATION #
#####################################################################

# Take permissions and groups from backend server instead of proxy
use-bukkit-permissions-manager: false

# Sometimes server might be using offline uuids in tablist instead of online, such as disabling waterfall's tablist rewrite option
# If you experience tablist formatting not working, toggle this option (set it to opposite value)
# Only affects proxies with online mode enabled
use-online-uuid-in-tablist: true
